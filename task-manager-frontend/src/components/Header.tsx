import React from 'react';
import './Header.css';
const Header: React.FC = () =>{
     return (
    <header className="header">
      Task Manager {/* Text is now directly inside the header */}
    </header>
  );
};
export default Header;